{
  "sweep_id": "SW-0007",
  "title": "ATR volatility breakout on BTC and ETH spot, hourly",
  "hypothesis": "Volatility breakout rules produce no risk-adjusted edge after realistic costs, and no detectable directional tilt across the family.",
  "search_space": {
    "families": [
      "atr_breakout"
    ],
    "parameters": {
      "lookback": [
        10,
        20,
        30,
        50,
        80,
        120
      ],
      "entry_threshold": [
        1.0,
        1.5,
        2.0,
        2.5,
        3.0
      ],
      "exit_rule": [
        "mean_touch",
        "opposite_band",
        "fixed_n_bars"
      ],
      "holding_cap_bars": [
        4,
        12,
        48
      ]
    },
    "n_trials": 540,
    "enumeration_note": "5 entry_threshold x 3 exit_rule x 3 holding_cap_bars x 6 lookback x 2 symbols = 540 fully enumerated variants. No variant is excluded after the fact. entry_threshold is the number of ATRs from the rolling mean."
  },
  "data": {
    "symbols": [
      "BTCUSDT",
      "ETHUSDT"
    ],
    "start": "2017-09-01",
    "end": "2025-06-30",
    "timeframe": "1h",
    "source": "binance public klines via data.binance.vision, see data/manifest/",
    "holdout": "2025-07-01 onward — not loaded or inspected until the verdict is written"
  },
  "protocol": {
    "method": "Fixed rules, no in-sample parameter fitting. Every variant evaluated over the full period; in-sample/out-of-sample splitting is done by CSCV at scoring time.",
    "costs_bps": 4.0,
    "slippage_bps": 2.0,
    "execution_model": "signal from bar t close, held over bar t+1, no partial fills, position capped at 1x notional per side",
    "survivorship_note": "symbols fixed at the start of the period, no delisting selection"
  },
  "decision_rule": {
    "metric": "deflated_sharpe_ratio",
    "alpha": 0.05,
    "threshold": 0.95,
    "note": "Fixed before any result was computed. Not adjustable. Run diagnose.py --null afterwards for the cross-sectional picture; that is exploratory here and must be labelled as such."
  },
  "planning": {
    "periods_per_year": 8760,
    "n_observations": 68490,
    "assumed_var_trial_sharpes": 1.8e-05,
    "assumed_skewness": -0.2,
    "assumed_kurtosis": 8.0,
    "power": 0.8,
    "cscv_splits": 16,
    "sd_source": "variance of trial Sharpes assumed from SW-0002; the realised value was ~3.5x larger, which raises the bar rather than lowering it"
  }
}